version: 0.2

phases:
  install:
    commands:
      - echo "Skipping build. Just packaging JAR."

  build:
    commands:
      - echo "Nothing to build. JAR already exists."

  post_build:
    commands:
      - echo "Build done. Proceeding to artifacts upload."

artifacts:
  files:
    - build/libs/student-admission-app-1.0-SNAPSHOT.jar
